sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t masbot/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true masbot/docker-react npm run test -- --coverage
